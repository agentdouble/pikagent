# Project Memory

- UI settings use `persistedSetting` for localStorage-backed values. Settings sections are registered through `registerComponent` and loaded via side-effect imports in `src/components/index.js`.
- Explorer file rows should reuse `src/utils/file-icons.js` as the single source of truth for file language detection and visible file-type icons.
- The Board should act as an agent control table with lightweight output previews and actions; do not embed full xterm instances in each Board card.
- Board previews should prefer the source terminal xterm buffer over raw PTY text when available, because agent TUIs redraw with carriage returns/cursor control sequences that garble lightweight raw previews.
- Board previews should extract only the latest agent answer from Codex-style terminal chrome: keep the newest `• ...` response block and cut off later `› ...` prompts, separators, and model/status lines like `gpt-5.5 xhigh fast · ~`.
- Board preview text should stay stable during transient agent states: keep showing the previous real answer and render `Working`, `Thinking`, or `Messages` underneath it instead of replacing the answer; continue suppressing prompt templates and status-only redraws.
- Branch badge sync: terminal branch refresh is wired through `src/utils/terminal-events.js` as `terminal:branchCheck`; `tab-lifecycle.js` performs the injected `gitBranch` lookup. Keep terminal code free of direct `window.api.git` calls.
- Workspace numeric shortcuts: default direct tab shortcuts are `control+1` through `control+9` with action ids `goToTab1` through `goToTab9`. Direct lookup uses visible tab order and skips tabs marked `NoShortcut`; xterm must let these combos bubble to `ShortcutManager`.
- Linear-inspired theme: global restyle overrides live in `src/styles/linear-theme.css`, loaded last from `src/index.html`. Keep future visual polish there when possible, and keep the default `Pikagent` terminal theme aligned with the same neutral graphite palette in `src/utils/terminal-themes.js`.
- Theme color preference: Jeremy found the first Linear-inspired dark background too black. Prefer a softer pastel slate/lavender dark palette over near-black graphite for app backgrounds.
- Theme border preference: Jeremy found the Linear-inspired UI too boxed in when separators were highly visible. Prefer low-alpha subtle borders and softer active terminal/tab accents instead of strong panel outlines.
- Self-update uses `electron-updater` against GitHub Releases (`agentdouble/pikagent`) and should consume published macOS artifacts (`dmg`, `zip`, `latest-mac.yml`), not rebuild locally from a source checkout.
- Manual local machine update from this source checkout: validate with `npm test`, run `npm run package`, quit `/Applications/Pickagent.app`, replace it with `release/mac-arm64/Pickagent.app`, run `codesign --verify --deep --strict /Applications/Pickagent.app`, remove quarantine if present, then relaunch.
- macOS release builds must keep `build.mac.notarize: true` and `hardenedRuntime: true`; GitHub release workflow expects signing/notarization secrets before pushing a public `v*` tag.
- As of 2026-06-12, local `npm run package:dmg -- --publish never` produces only arm64 artifacts and falls back to ad-hoc signing, so public "all Mac" deployment still needs Developer ID signing/notarization plus either universal or separate x64/arm64 release artifacts.
- As of 2026-07-05, `agentdouble/pikagent` is public, default branch `main` has the active `Release` workflow, `gh release list` and local/remote `v*` tag checks are empty, and repo secrets only include `CLAUDE_CODE_OAUTH_TOKEN`; public macOS distribution still needs `CSC_LINK`, `CSC_KEY_PASSWORD`, `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, and `APPLE_TEAM_ID` before pushing the first `v*` tag.
- As of 2026-07-05, the GitHub release secrets are present by name and local `codesign` finds `Developer ID Application: COLLOVRAY JEREMY (LJ44JWQ586)`; local signed packaging can still fail with `ambiguous` if the same certificate exists in both `/Library/Keychains/System.keychain` and `~/Library/Keychains/login.keychain-db`, while GitHub Actions should import only the `.p12` secret into its runner keychain.
- macOS public releases should build separate `x64` and `arm64` DMG/ZIP artifacts through `build.mac.target` rather than relying on the runner's current architecture; unsigned local x64 packaging with `CSC_IDENTITY_AUTO_DISCOVERY=false` succeeded after rebuilding `node-pty` for x64.
- The first `v1.0.0` release run on 2026-07-05 used GitHub's `macos-15-arm64` runner via `macos-latest`; it passed tests, rebuilt `node-pty` for x64, began signing `release/mac/Pickagent.app`, then hung in `notarytool` for about 24 minutes before manual cancellation. Prefer `macos-15-intel` for the combined x64+arm64 release job so x64 signing/notarization is native while keeping one combined `latest-mac.yml`.
- The corrected `v1.0.0` release run on 2026-07-05 succeeded on `macos-15-intel`; `notarytool` stayed silent for most of the 46-minute job, then GitHub published non-draft release assets for Intel (`Pickagent-1.0.0.dmg`, `Pickagent-1.0.0-mac.zip`), Apple Silicon (`Pickagent-1.0.0-arm64.dmg`, `Pickagent-1.0.0-arm64-mac.zip`), blockmaps, and `latest-mac.yml`.
- The `v1.0.1` release run on 2026-07-06 succeeded in 11m30s on `macos-15-intel` from commit `48110a6`, publishing non-draft Intel and Apple Silicon DMG/ZIP assets plus blockmaps and `latest-mac.yml`; this release ships the real bundled `pickagent` workflow skill and migrates the legacy placeholder.
- App icon assets are generated from `assets/pickagent-source.png` by `scripts/build-icon.py`; the default generated background is now black (`#000000`) across `assets/pickagent.png`, `assets/icon.iconset/*`, and `assets/icon.icns`.
- PTY agent detection uses `pgrep -P <pid>`; exit code `1` means no child process was found and should be treated as an empty result, not logged as `_checkAgent failed`.
- Board replies should write the message text and terminal Enter as two separate PTY writes (`text`, then `\r` after a short tick), not one combined `text\r` payload; combined writes can fill the agent prompt without submitting it.
- Board reply controls should use explicit button `onClick` plus input `keydown Enter` handlers for PTY writes; avoid relying on a form submit path in the Electron Board.
- Dev Electron must use a checkout-scoped `Pickagent Dev/<checkout>` userData profile and `npm run dev` must stay foreground-managed; backgrounding `node build.js --watch & electron . &` leaves orphan watchers and can collide with production Chromium storage.
- Git branch checks may run in terminal cwd values that are not repos, such as `/Users/jeremy`; `git rev-parse` non-repo errors should return `null`/`false` quietly instead of logging repeated `[git-manager]` warnings.
- Board agent cards should stay visually minimal and square: visible content is the agent output plus the reply input, with waiting/running state communicated by card color instead of labels, summaries, or action chrome.
- Board now separates terminal-attached agents from headless agents: terminal cards come from `ptyCheckAgents`, headless cards come from the `agents:list` process monitor (`codex exec`, `claude -p/--print`, `opencode -p`, or `run_headless_agent.py`) and each group can be hidden independently.
- Board headless detection must group a shell -> node wrapper -> agent binary chain into one card by root headless PID when no explicit log/cwd key exists; otherwise one `codex exec` run appears as three duplicate cards.
- Boucles displays the shared headless process monitor through `loopFacade.headlessList/headlessKill`; linked agents are grouped by `loopBoardId` in the active board rail, while unrelated headless processes stay visible under "Autres headless".
- Boucles headless rail visibility is persisted in localStorage with `pickagent.loop.headlessPanelCollapsed`; when collapsed and no inspector is open, the right rail is removed so the board uses the full width.
- Skills root configuration should offer two choices at click time: browse to a folder with the native folder picker, or manually enter/paste a filesystem path.
- Agent availability indicators in top workspace tabs are live state, not workspace config: `AgentTabStatusTracker` polls `ptyCheckAgents`, reuses Board's data-volume threshold, stores per-terminal status in `tab.agentStatuses`, and renders the green dot only when an agent is waiting for input.
- Packaged app startup installs a bundled `pickagent` skill into both `~/.codex/skills/pickagent/SKILL.md` and `~/.claude/skills/pickagent/SKILL.md`; it is now a real workflow-builder guide for Boucles/Flows, hook contracts, nodes, validation, and product framing.
- Flow hook parity with IAFlow: Pickagent flows now support `triggerType: "hook"` + `hookTrigger` (`event`, `provider`, `paths`, `debounceSeconds`), cards format hooks via `src/utils/flow-trigger-helpers.js`, scheduler ignores hook flows, and `bin/pickagent-hook.cjs` emits/runs hook-triggered flows from `~/.config/.pickagent/flows/` while writing headless logs/runs back into the Flow UI.
- Boucles tab parity with IAFlow: the visual loop board uses a separate `loop:*` IPC domain, supports multiple named boards persisted as JSON files under `~/.config/.pickagent/loops/`, stores per-board node logs under `~/.config/.pickagent/loops/logs/<boardId>/`, auto-saves board edits, runs agent nodes through Pickagent's existing `buildFlowCommand`, and exposes hook-triggered loop agents to `bin/pickagent-hook.cjs` alongside regular flows.
- The local `/Users/jeremy/lab/watcher` `task.yaml` watcher must trigger Pickagent Boucles, not IAFlow: its `config.yaml` uses `pickagent_hook.hook_bin: /Users/jeremy/projet/pickagent/1-pikagent/bin/pickagent-hook.cjs`, event `orchestre`, cwd `/Users/jeremy/lab/orch`, and currently matches loop agent `loop:main:node-10-1781913986773-a6wi9t`.
- Boucles board visual state: running agent nodes get the `is-running` class and a green card outline; selected nodes use a separate accent-colored ring so selection is not confused with runtime activity.
- Hook-launched Boucles agents are external to the Electron `LoopManager` process; `pickagent-hook` must persist their run state under `~/.config/.pickagent/loops/runs/<boardId>/<nodeId>.json` so the Boucles snapshot can show the node as running and allow Stop.
- Internally launched Boucles agents must also persist the same loop run state with their shell PID and `logFile`; the Board headless monitor resolves those active run-state files by PID so headless cards can display Boucles logs even when the agent command has no `--log-file` argument.
- Boucles `Fichier` nodes open their configured `filePath` through `loopFacade.openPath`; keep the button disabled when the path is empty and surface Electron `openPath` error text in the LoopView error banner.
- Boucles agent trigger modes include `schedule`, `hook`, and `link`; `link` agents are launched only from incoming visual edges after the upstream runnable node exits cleanly, and chain execution must guard against cycles with a visited set.
- Boucles Codex agent nodes support per-node `model`, `reasoningEffort`, and `serviceTier`; empty values inherit global Codex config, while runs inject `--model`, `-c model_reasoning_effort=...`, and `-c service_tier=...` through `buildFlowCommand`.
- Boucles `Run pipeline` launches eligible root nodes while skipping any node already running; link-triggered agents are not started directly because upstream success launches them through directed edges.
- Boucles `Run executables` launches only enabled executable/watcher nodes on the active board, skips nodes already running, and disables link-following so it does not start downstream agents.
- Boucles links are directed edges (`from -> to`) with a UI reverse action; `Run pipeline` starts directed graph roots, then follows links to executable targets and agents in `triggerType: "link"` without relaunching already-running nodes.
- Boucles visual edges should feel like simple Excalidraw-style connectors: node cards expose four port buttons on hover/selection, links are created by choosing a source port then a target port, a global header button toggles `curve` vs `elbow` routing, and links themselves are clickable/selectable.
- Boucles canvas zoom supports mouse wheel zoom anchored under the cursor; keep zoom bounds and pan adjustment centralized in `clampZoom` and `zoomAtPoint` from `src/utils/loop-view-helpers.js`.
- Boucles viewport state (`zoom` + `panOffset`) is a renderer-local preference persisted per board in localStorage under `pickagent.loop.boardViewport.<boardId>`; do not write this UI-only state into the loop board JSON.
- Boucles `Stop pipeline` stops every currently running agent/executable node on the active board, including disabled-after-launch nodes and hook-launched external loop runs.
- The `software engineering` Boucles board has an unblocker lane for blocked orch tasks: executable node `node-13-1782020000000-unbexe` runs `/Users/jeremy/lab/watcher/start-unblocker.sh`, hook agent `node-14-1782020001000-unbagt` listens on event `lanceur.unblocker`, and the agent uses global skill `unblock-orchestre-tasks` to recover `launch_failed`, `review_launch_failed`, `merge_failed`, or `blocked` tasks safely.
- The `software engineering` scheduled agent `verif pipeline` (`node-11-1781997196796-qzsf4t`) uses global skill `verify-orchestre-pipeline` from `~/.codex/skills/verify-orchestre-pipeline`; its helper dry-runs `/Users/jeremy/lab/watcher/start-unblocker.sh`, `start.updater.sh`, `start.sh`, `start-coder.sh`, `start-rework.sh`, `start-reviewer.sh`, `start-mergeur.sh`, `start-ideer.sh`, `start-ideer-ux.sh`, and `start-journey-tests.sh`, then emits only matched Pickagent hooks when run with `--trigger`.
- The `software engineering` `ideer` lane now supports per-project task caps from `/Users/jeremy/lab/orch/projet.yaml`: `ideer_task_limit` controls how many compact active tasks the `alimenteur-task-yaml` skill may keep for that project, defaulting to `3`; the watcher triggers when `task.yaml` is below that limit.
- The global skill `task-yaml-bridge` lives at `~/.codex/skills/task-yaml-bridge`; use it for conversational user requests that should append compact source strings to `/Users/jeremy/lab/orch/task.yaml` after resolving the project through `projet.yaml`, while `alimenteur-task-yaml` remains the autonomous idea-generation skill.
- The global skill `orchestre-daily-status` lives at `~/.codex/skills/orchestre-daily-status`; use it to report, by project, what merged today from `orchestre-finished.yaml` and what is currently executing from `orchestre.yaml`, using Europe/Paris local dates by default.
- The `software engineering` Boucles board now has a journey testing lane: scheduled agent `Journey generator` uses `~/.codex/skills/journey-test-generator` to maintain `journeys.yaml`, executable `Watcher journeys` runs `/Users/jeremy/lab/watcher/start-journey-tests.sh`, hook agent `Journey tester` listens on `journey.test` and uses `~/.codex/skills/journey-test-runner`, and scheduled agent `Journey issue fixer` uses `~/.codex/skills/journey-issue-fixer` to route reproducible journey failures back into `task.yaml`.
- Codex global MCP server `devtools` is configured in `~/.codex/config.toml` as `npx -y chrome-devtools-mcp@latest --headless --isolated --no-usage-statistics`; journey testing should prefer DevTools MCP over Playwright for browser E2E work and keep Chrome silent/headless by default.
- Packaged Pickagent installs bundled skills from `main/bundled-skills` through `installBundledSkills` into both `~/.codex/skills` and `~/.claude/skills`; the installer migrates only the exact legacy `pickagent` placeholder (`*`) to the real guide and leaves custom `pickagent` skills plus their companion files untouched. `software-engineering-daily-report` remains the bundled daily pipeline recap skill for merged PRs by project.
- For a future UX/design agent in the `software engineering` Boucles board, the strongest insertion point is before `Codeur`: the current coder watcher launches on `orchestre.yaml` tasks with `status: todo`, so a real pre-code UX gate would need a dedicated watcher/event/status such as `ux_todo` -> `ux_ready` -> `todo`. A second, lighter UX review lane can also inspect `pr_opened` frontend PRs, but that should complement rather than replace the pre-code design gate.
- The desired UX lane should have two specialized agents using the global `ui-ux-pro-max` skill: an autonomous `Ideer UX` first, which inspects active applications and proposes UX/product-polish tasks into `task.yaml` or a UX backlog, and a dedicated `Codeur UX` later, which implements only UX/UI tasks after design framing instead of mixing visual quality work into the generic Codeur.
- The `software engineering` Boucles board now has an `Ideer UX` hook lane: watcher entrypoint `/Users/jeremy/lab/watcher/start-ideer-ux.sh`, config sections `ideer_ux_watcher` and `ideer_ux_pickagent_hook`, event `ideer.ux`, Pickagent target `loop:main:node-20-1783000001000-uxagt`. The watcher uses `ideer_task_ux_limit` per active project, counts compact `task.yaml` tasks prefixed `[UX]`/`[UI]`/`[Design]` or quoted `UX:` variants, and emits `source: task_yaml_ideer_ux_watcher`.
- For spec-heavy projects such as `/Users/jeremy/projet/aide-domicile`, the `software engineering` ideer lanes should become spec-first: add a project-level `spec_path`/`spec_paths` in `/Users/jeremy/lab/orch/projet.yaml`, make `alimenteur-task-yaml` and the UX ideer wrapper read those specs before inventing tasks, generate missing implementation tasks from uncovered spec requirements first, and only propose new product/UX ideas once the spec is already fully represented in `task.yaml`/`orchestre.yaml`/finished work.
- Implemented spec-first ideer direction: `/Users/jeremy/lab/orch/projet.yaml` can now declare `spec_path` for active projects such as `aide-domicile`; global skill `alimenteur-task-yaml` documents spec-first generation; new global skill `alimenteur-ux-task-yaml` handles the `ideer.ux` lane with `[UX]` tasks; Boucles node `Ideer UX` now invokes that wrapper instead of using `ui-ux-pro-max` directly.
- The global `unblock-orchestre-tasks` helper now supports `--include-manual` for the narrow case where the user explicitly fixed the external cause behind `manual_required: true`; use it with `--project-id` or `--task-id`, as in the `aide-domicile` recovery after the project gained a GitHub remote, then verify with `start-coder.sh --once --dry-run --json --force`.
- To reduce software-engineering Boucles token usage, the coder watcher now emits a compact `lanceur.coder` payload with only `project_id`/`task_id`, supports `start-coder.sh --summary`, and the Codeur node must invoke `launch_orchestre_agents.rb` with `--project-id`/`--task-id` filters so it reads only selected tasks from `/Users/jeremy/lab/orch/orchestre.yaml`.
- 2026-07-02 token review for the `software engineering` Boucles board: next token-reduction targets are compact hook payloads and filtered launchers for `reviewer`, `mergeur`, `rework`, `journey.test`, `ideer`, and `ideer.ux`; avoid injecting full journey steps/assertions, review issue details, full specs, or large loop logs into node prompts when `project_id`/`task_id` or `journey_id` can let the skill reload only the targeted data. Recent node logs showed high-token runs around `journey.test` (~192k-220k), `ideer` (~191k), `mergeur` (~146k), `codeur` (~138k), and `reviewer`/`ideer.ux` (~70k), so prioritize journeys/spec ideation/merge-review before micro-optimizing short Boucles prompts.
- 2026-07-02 token hygiene applied: the live `software engineering` board keeps `Codeur`, `Reviewer`, and `Rework` at `reasoningEffort: xhigh`, while non-code/review agents such as `Mergeur`, `ideer`, `Archive merged`, `Unblocker`, and journey agents now run with `reasoningEffort: low`. The Boucles log panel must not read whole node logs; `LoopManager.getNodeLog` and snapshot `lastLogLines` now read a bounded tail instead of loading multi-MB files into the renderer.
- 2026-07-02 Usage token tracking: `Usage > Tokens` now augments Claude project token totals with Boucles/Flow log summaries by parsing `session id` + `tokens used` lines, deduplicating copied session output across executable watcher logs and agent logs, and ranking both per consumer (`perTokenConsumer`) and per individual session (`perTokenSession`).
- 2026-07-02 Codex quota display: `Usage > Tokens` also reads the latest local Codex `token_count` event from recent `~/.codex/sessions/**/*.jsonl` tails, exposes `codexUsage` with `primary` and weekly `secondary` remaining percentages, and the Usage view refreshes automatically every 60 seconds while the manual Refresh button forces the metrics cache to clear.
- 2026-07-02 reviewer launcher cost change: the live `software engineering` Boucles `Reviewer` node (`node-6-1781979795618-dojpw4`) now runs with `reasoningEffort: low` because it only launches `launcher-review`; keep review quality in the delegated review agents/prompts, not in the launcher.
- 2026-07-02 worktree launch safety: global skill `execute-orchestre-tasks` now syncs the source project before creating a new task worktree: `launch_orchestre_agents.rb` runs `git fetch --prune` then `git pull --ff-only`, fails launch if the pull cannot fast-forward, and creates new task branches from the updated upstream ref instead of stale local `HEAD`.
- Terminal viewport sizing: keep visual padding on `.terminal-container .xterm`, not on `.terminal-container`. `@xterm/addon-fit` measures the parent height but subtracts only `.xterm` padding, so parent padding can make xterm calculate one row too many and clip the prompt at the bottom.
- 2026-07-07 Windows compatibility pass: cross-platform shell/process behavior is centralized in `main/platform-helpers.js` and `main/process-helpers.js`; flows/Boucles/hook runs use PowerShell-safe quoting/spawn on `win32`, PTY/headless detection avoids POSIX-only `pgrep`/`ps`/`lsof` on Windows, and release packaging targets Windows x64 only for now because `node-pty` native rebuilds make macOS cross-builds fail (`node-gyp does not support cross-compiling native modules from source`).
- 2026-07-07 Windows verification hardening: `npm run package` now dispatches through `scripts/package-current.js` to `package:win` on Windows and `package:mac` elsewhere; `.github/workflows/windows-compatibility.yml` is the branch/PR CI that must prove `npm ci`, `npm test`, `npm run build`, and `npm run package:win` on `windows-2022`; the bundled daily-report skill defaults to `$PICKAGENT_ORCH_ROOT` or `~/lab/orch` instead of a `/Users/jeremy/...` path.
- 2026-07-30 Windows CI packaging: keep `windows-2022`, which detects Visual Studio 2022 without an npm override. Node 24 ships an npm version that rejects `npm config set msvs_version 2022` with `msvs_version is not a valid npm option`, so release and compatibility workflows must call `npm ci` directly.
- 2026-07-30 Windows releases are tag-driven through the `windows` job in `.github/workflows/release.yml`. The temporary branch-triggered `windows-release-assets.yml` backfill was removed before `v1.0.2` because it targeted the already-published `v1.0.1` release and could overwrite its assets.
- 2026-07-07 SSH explorer support: terminal `pty:getCwd` detects a direct child `ssh <destination>`, resolves the remote login directory through a separate non-interactive `ssh -o BatchMode=yes <destination> pwd -P`, and exposes it to the renderer as `ssh://<encoded-destination>/<remote-path>`; file-tree browsing/read/write/create/rename for those URIs is routed through `main/ssh-fs-manager.js`.
- 2026-07-07 Flow JSON output fix: Codex flows run with `codex exec --json` and `main/flow-stream-parser.js` formats Codex `response_item`/`message`/`result` JSONL events; the Flow view should only poll every 2s while runs are active to avoid repeatedly reloading completed inline logs.
- 2026-07-07 Skills paths: the Skills view supports multiple roots via `skills-settings.json` fields `roots` and `activeRoot`, while legacy `root` settings are migrated in memory; defaults are `~/.codex/skills`, `~/.claude/skills`, and `~/.opencode/skills`, and create/import writes to the active root while listing aggregates every configured root.
